import { StyleSheet, Dimensions, StatusBar } from 'react-native';



export const styles = StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        backgroundColor: 'rgba(42,179,234,0.95)'
    },
});